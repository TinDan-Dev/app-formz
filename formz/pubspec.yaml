name: formz
description: Generalizes form validation

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: '1.0.11'

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_lints: '>=1.0.4'
  
  # for state management
  flutter_bloc: ^8.0.1
  equatable: ^2.0.3
  freezed_annotation: ^1.0.0

  # stream utils
  rxdart: ^0.27.1
  
  matcher: ^0.12.11
  stack_trace: ^1.10.0
  meta: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.0.5

  freezed: ^0.14.2

  source_gen: ^1.1.0
  code_builder: ^4.0.0

  bloc_test: ^9.0.3
  mockito: ^5.0.10

  formz_dev:
    git: 
      url: https://git@github.com/TinDan-Dev/app-formz.git
      path: formz_dev
