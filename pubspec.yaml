name: formz
description: Generalizes form validation

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: '1.0.4'

environment:
  sdk: '>=2.13.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # for state management
  flutter_bloc: ^7.0.0
  equatable: ^2.0.3
  freezed_annotation: ^1.0.0

  # stream utils
  rxdart: ^0.27.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.0.5

  freezed: ^0.14.2

  source_gen: ^1.1.0
  code_builder: ^4.0.0

  bloc_test: ^8.1.0
  mockito: ^5.0.10

  formz_test:
#    path: ../app-formz-test
    git: https://git@github.com/TinDan-Dev/app-formz-test.git

